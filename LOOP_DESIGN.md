# converge：质量收敛循环设计

> 目标：把 [EVOLUTION_PLAN.md](EVOLUTION_PLAN.md) 的 16 项改进，做成一个**每轮一个动作、验证器把关、可中断可恢复**的循环，直到全部收敛。
> 参照系：8/3 晚 Claude Code 的"缩进修复循环"（9 次提交收敛到 "every Python block compiles"）证明了这个模式可行；那个 loop 暴露的两个教训，本设计显式修复。

---

## 1. 为什么是 loop，而不是一次干完

- 每一项改进都要求"可验证、可回退、可审"：一次干 16 项，任何一步出错都难定位。
- loop 每轮只做一个动作 + 一个提交，任何一步失败都只影响一步，`git log` 就是审计轨迹。
- 中断可恢复：进度文件 + 提交历史双保险，下次从"下一个 pending"继续。

**上一个 loop 的两个教训，本设计如何修复：**

| 教训（worktree 残留） | 本设计对策 |
|---|---|
| 第二个任务做到一半中断，4 个文件未提交、无任何记录 | `scripts/loop/progress.md` 每轮必写：完成/进行中/阻塞，中断后从文件恢复 |
| 没有失败策略，卡住就停 | 同一动作失败 2 次 → 标 `blocked` 记原因，跳到下一项，最终输出收敛报告 |

---

## 2. 六要素规格

| 要素 | 定义 |
|------|------|
| **目标状态** | 见 §2.1 目标三层结构：**结果目标**（内容指标达阈值）+ **约束**（verify_content 全 0）+ **前置**（机制就绪）。收敛判据 = 结果目标达成且约束不破，不是 backlog 清空 |
| **动作空间** | backlog 中下一个 `pending` 项（优先级 P0 → P1 → P2），每轮恰好一个动作，最小改动 |
| **验证器** | 双验证器：`verify_content.py --list`（**底线**，防变坏）+ `--metrics`（**目标度量**，测变好，迭代 0 新增）+ 回归检查 + `git diff` 人工可审 |
| **失败策略** | 同一动作失败 2 次 → `blocked` + 原因，跳过；无法自动验证质量的项降级为"最小可验证子目标"（如翻译质量 → 术语表一致性） |
| **资源预算** | 每轮 1 个动作；迭代上限 16 轮（= backlog 大小）；超限输出收敛报告，不再自动继续 |
| **进度持久化** | `scripts/loop/progress.md`（每轮更新）+ git 提交历史（每轮一个提交，可 `git revert`） |

### 2.1 目标三层结构（修正版）

上一版把"backlog 清空 + 检查全 0"当作目标，这是**过程目标**：16 项里 12 项是机制，做完机制不等于内容变好；且 `verify_content.py` 十项检查全是负向约束（无死链、无未来源数字、无编号断裂），只能证明"不坏"，不能证明"更好"。修正如下：

| 层 | 内容 | 角色 |
|----|------|------|
| **结果目标**（收敛判据） | 内容指标达到阈值：术语表 ≥ 100 条 · skills ≥ 5 个 · 失败案例 ≥ 2 篇 · Prompt 模板库可复算 · 无过期 verified 标记 · README 数字全部可复算 | 内容优化的**可观测结果**，由 `--metrics` 度量 |
| **约束**（底线） | `verify_content.py` 十项检查全为 0，任何动作不得引入新失败 | 内容"不烂"的底线，**不是**目标 |
| **前置**（手段） | M7 顶层检查、过期门禁、CHANGELOG 门禁、外链 cron 等机制项 | 为内容优化铺路；**单独完成它们不算收敛** |

**关键推论**：没有度量就没有收敛判据。所以 backlog 新增**迭代 0**——给 `verify_content.py` 加 `--metrics` 内容度量模式（输出上表"结果目标"各指标），它是一切的前提。loop 的收敛定义因此变为：

> 收敛 = 内容指标达到阈值 **且** verify_content 全 0 **且** 前置机制就绪。

而不是"16 项清空"。

---

## 3. 每轮循环步（Playbook）

```
┌─ 1 探测：git status 确认工作树干净（绝不覆盖用户未提交改动）
│          python3 scripts/verify_content.py --list   → 记录当前失败项
├─ 2 动作：读 progress.md，取下一个 pending 项，执行（只动必要文件）
├─ 3 验证：重跑验证器 → 目标项归零 + 无新失败（回归）
│          若失败：修；再失败 1 次（累计 2 次）→ 标 blocked，跳到下一项
├─ 4 提交：git commit -m "loop(converge): <动作编号> <动作名>"（一步一提交）
├─ 5 记录：更新 progress.md（done/blocked + 本轮验证结果快照）
└─ 6 预算：剩余迭代 = 16 - n；为 0 或 backlog 清空 → 输出收敛报告，终止
          否则回到 1
```

**完成标志（每轮输出）**：本轮 diff 摘要 + 验证结果（X 项失败） + 剩余项（n/16）。

---

## 4. Backlog 与迭代序列

> 角色标注：**[内容]** = 直接产出内容资产（结果目标）· **[门禁]** = 内容质量的约束检查（底线）· **[前置]** = 为内容优化铺路的机制（手段，完成不等于收敛）。

| 迭代 | 动作 | 角色 | 验证方式 | 依赖 |
|------|------|------|----------|------|
| 0 | **R-0** `verify_content.py` 新增 `--metrics` 内容度量模式（术语表/skills/案例/模板/过期标记/README 可复算指标） | 前置 | `--metrics` 可运行、输出六项指标 | — |
| 1 | **R-1a** README 数字单一数据源：章节数从 `src/SUMMARY.md` 统计、Prompt 数从模板库统计、Notebook 数从 `notebooks/*.ipynb` 统计 | 前置 | 脚本输出与 README 声称一致 | — |
| 2 | **R-1b** `verify_content.py` 新增 M7（顶层文档树：README/README_EN/README_JA/roadmap 纳入锚点/链接/编译检查）+ README 数字统一 | 前置 | M7 通过，330/380 矛盾消失 | 1 |
| 3 | **R-2** 修复或退役 `tests/`：迁移到 mdBook 时代（SUMMARY 完整性、三语数量、verify_content 可运行），CI 加 pytest 步骤 | 前置 | pytest 全绿且 CI 跑 | — |
| 4 | **U-1** verified 过期门禁：`claims: verified YYYY-MM` 距今 > 6 个月 → 失败 | 门禁 | M1 扩展项通过 | 0 |
| 5 | **R-5** M2 升级：失效小节正文须含具体失效条件信号（数字/规模/品类/平台词），纯免责声明报错 | 门禁 | M2 通过 | — |
| 6 | **R-6** 无证据断言检查：含平台名/年份/功能名、无来源无标记的陈述 → 报错 | 门禁 | 新检查通过 | — |
| 7 | **R-7** EVOLUTION_LOG 对齐：重写为基于 verify_content 实际指标的状态报告 | 前置 | 文件内容与门禁输出一致 | 4 |
| 8 | **U-2** 外链 cron workflow：weekly 跑 `--probe-links`，死链自动开 issue | 前置 | workflow 文件存在 + 本地 dry-run 通过 | — |
| 9 | **U-3** CHANGELOG 门禁：内容 PR 必须带 [Unreleased] 条目 | 前置 | CI 检查上线 | — |
| 10 | **RICH-5** Prompt 模板库 `src/resources/prompt-library.md` + 自动计数 | 内容 | 计数与 README 声称一致；`--metrics` 模板数达标 | 1 |
| 11 | **RICH-2** 三语术语表初版（100 条：电商 AI 高频术语） | 内容 | `--metrics` 术语表 ≥ 100；锚点通过 | — |
| 12 | **R-4** 翻译 parity 升级：标题出现术语表词条须用对应译文 | 门禁 | parity 新检查通过；术语一致性命中率 100% | 11 |
| 13 | **RICH-1** `skills/` 资产：5 个场景技能包（选品/Listing/差评/合规/定价） | 内容 | `--metrics` skills ≥ 5；每个含 SKILL.md + 护栏 | — |
| 14 | **RICH-3** 失败案例 ×2（编造销量压错库存、自动调价翻车） | 内容 | `--metrics` 案例 ≥ 2；带 claims 标记 | — |
| 15 | **RICH-4** 交互式导航（角色→路径→Notebook→skills） | 内容 | 本地可打开、无死链 | 13 |
| 16 | **U-4/U-5** 易腐事实清单 + 读者过期报告 Issue 模板 | 前置 | 文件存在 + 模板语法正确 | 4 |

**收敛判定（每轮结束跑 `--metrics`）**：结果目标六项全部达标 且 `verify_content.py --list` 全 0 → 输出收敛报告，终止。

---

## 5. 终止与恢复

- **正常终止**：backlog 全 done 且验证器全 0 → 输出收敛报告（完成项、验证结果、残留风险），loop 关闭。
- **中断恢复**：任何时刻停掉，下次启动只需读 `progress.md`——`done` 跳过、`blocked` 说明原因、`pending` 继续。恢复成本 O(1)，不需要重放历史。
- **超限终止**：迭代到 16 轮仍有未完成 → 输出收敛报告（X done / Y blocked / Z pending + 原因），由人决定是否扩预算。

---

## 6. 运行载体（三选一）

| 载体 | 方式 | 适用 |
|------|------|------|
| **A 手动（推荐）** | 每轮结束停下，用户说"继续"再跑下一轮 | 修改仓库、想每步审 diff；迭代 1-2 轮最稳 |
| B cron 定时 | 每 2 小时跑一轮（agent payload），收敛后自动 disable | 想无人值守，但仓库修改自动化有风险 |
| C 一次性收敛 | spawn 子 agent 连续跑到 16 轮上限 | 信任验证器、追求一次跑完 |

**推荐 A**：前几轮（R-1/R-2）会动 README 和 scripts，值得人工过目；跑顺后第 4 轮起可切 C。

---

## 7. 安全边界（写进每轮指令）

1. **绝不覆盖用户未提交的改动**：每轮先 `git status`，有未提交改动 → 停下询问，不自动处理。
2. **一个动作一个提交**：禁止把多项改动揉进一个提交。
3. **验证器是唯一真源**：某动作让现有检查新增失败 → 该动作视为失败，先修回归再谈新功能。
4. **不编造验证结果**：`verify_content.py` 的退出码和输出为准，没有"我觉得应该能过"。
5. **blocked 不硬闯**：同一动作失败 2 次就记录原因跳过——留给人工，不让循环空转。

---

## 8. 复用：loop 模板不止用于这一次

收敛完成后，同一个模板（目标状态 + 验证器 + 动作空间 + progress）可以重新指向下一个目标：

- **月度保鲜循环**：目标 = 无过期 verified 标记 + 外链缓存 30 天内刷新；动作空间 = 刷新/复核/降级；验证器 = U-1 门禁。
- **内容扩充循环**：目标 = 术语表 100 → 300 条、skills 5 → 12 个；动作空间 = 按目录逐个补；验证器 = M7 + parity。
- **社区 PR 清理循环**：目标 = 无超过 30 天的 open PR；动作空间 = 逐 PR 审/合并/关闭。

loop 的本质是：**把"持续改进"从意志力问题变成收敛问题**——定义目标、给验证器、设预算，剩下的交给循环。
