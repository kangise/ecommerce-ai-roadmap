# ecommerce-ai-skills 演进方案（2026-08）

> 基于 2026-08-06 对仓库的实际勘察（git 历史、scripts/verify_content.py、CI workflows、README/CONTRIBUTING/EVOLUTION_LOG、tests/），所有"缺口"均有文件级证据，不是泛泛建议。
>
> 排序逻辑：先修**已证实的错误**（严谨 P0）→ 补**机制盲区**（严谨 P1）→ 再谈内容扩充（丰富）与保鲜机制（update）。更新性不是"多写新内容"，而是让易腐事实有自动过期信号。

---

## 0. 现状基线（已核实）

| 资产 | 现状 | 评价 |
|------|------|------|
| 内容 | 中/英/日 各 75 个 md 文件；指南章 56 篇（6 大路径）+ 5 案例 + 6 资源 | 结构完整 |
| 质量门禁 | `verify_content.py` 十项检查（结构 5 + 门禁 M1/M2/M4/M5/M6），CI 卡部署 | 全库最值钱的资产 |
| 外链 | `link-status.json` 缓存 480 条，M4 合并前强制验活 | 机制好，但刷新靠人工 |
| 易腐事实 | `model-matrix.md` 单点维护 + 校验日期 2026-07-31；全库 verified 标记 21 处 | 模式优秀，但无过期提醒 |
| 测试 | `tests/` 18 个 pytest，引用已删除的 `_config.yml` | **死资产**（见 1.1） |
| 演进日志 | EVOLUTION_LOG.md 描述 3 月 Jekyll 旧结构（paths/、prompts/） | 已过时 |
| CHANGELOG | 停在 v1.2.0（2026-03-14），[Unreleased] 空 | 与 git 历史脱节 |

---

## 1. 严谨性（Rigor）

### 1.1 已证实的错误（P0，一两天内可修完）

**R-1 README 内部数字自相矛盾。**
- 第 13 行："330+ 个 Prompt"；第 51 行："380+ Prompt"。同一文件两个数字。
- 第 16 行 alt 文本"56 篇指南"（与指南章实际数 56 一致 ✓）；第 52 行"各 68 章"（与 75 个文件、56 篇指南都对不上）。
- 根因：`verify_content.py` 的 `TREES` 只覆盖 `src/`、`i18n/en/src`、`i18n/ja/src`，**README/README_EN/README_JA/roadmap 完全在门禁之外**——全库最显眼的文件反而没有校验。

**R-2 `tests/` 是死资产。** `test_repo_properties.py` 断言 `_config.yml` 存在（Jekyll 时代），该文件早已删除，仓库已迁移 mdBook。18 个 pytest 现在必挂，且 CI 从未跑过它们。"18 个测试全部通过"（EVOLUTION_LOG 声称）是假的。死资产比没有更糟——它让读者以为有测试保障。

### 1.2 机制盲区（P1，本轮演进的核心）

**R-3 顶层文档纳入校验（新增 M7）。** 在 `verify_content.py` 加一棵"顶层文档树"（README.md / README_EN.md / README_JA.md / roadmap/），复用现有锚点、链接、编译检查；并新增"数字声称一致性"检查：
- 单一数据源：章节数从 `src/SUMMARY.md` 自动统计；Prompt 数从模板目录（见 RICH-5）自动统计；Notebook 数从 `notebooks/*.ipynb` 统计。
- README 里出现的"56 篇 / 68 章 / 330+ Prompt / 18 个 Notebook"全部改为引用这些统计值，或由 M7 校验一致。数字声称要么可复算，要么不许写。

**R-4 翻译 parity 升级：从结构指纹到术语一致性。** 现有 parity 只比"标题层级序列、代码块数、表格行数"，检测不出"译文是错的"。"中英双语无翻译错误"是终局标准，但 75×3 = 225 个文件不可能靠人工全覆盖。可落地的最小版本：
- 维护一份三语术语表 `src/resources/glossary.md`（电商 AI 高频术语 100 条，中/英/日三列）；
- parity 检查新增：标题文本在译文里出现术语表词条时，必须用对应译文（正则 + 术语表查表，先只查标题，再下探正文）；
- 诚实标注边界：这是"术语一致性"检查，不是"翻译质量"检查——后者超出静态检查能力，交给社区审核。

**R-5 M2 从"标题存在"到"内容非空壳"。** 现在只查"什么时候这套不管用"标题在不在。升级：检查该小节正文是否含**具体失效条件**信号（数字、规模词、品类词、平台名）——纯免责声明（"请结合实际"）直接报错。用模式匹配，不装能理解语义。

**R-6 无证据断言检查（M1 的姊妹项）。** M1 只抓"硬数字"，抓不住"某平台 2026 年上线了 X 功能"这类**无数字但可证伪**的陈述。新增检查：无来源、无 claims 标记、无链接的可证伪断言（含平台名/年份/功能名），一律要求带出处或 claims 标记。避免"3 个月后整个句子过期"的隐性失真。

**R-7 EVOLUTION_LOG 与现状对齐。** 该文件由 AI 进化引擎维护，但描述的是 3 月的旧结构（paths/、prompts/、_config.yml），且"终局达成 100%"是自评。两个选择：(a) 重写为基于 `verify_content.py` 实际指标的状态报告（每项检查的通过数、verified 标记过期数）；(b) 标注"已归档"，把状态追踪迁到 CI 产物。推荐 (a)——它本身就是"严谨性"的示范。

---

## 2. 丰富性（Richness）

**RICH-1 skills/ 资产目录（最大差异化机会）。** README 已教"怎么把 Prompt 迁移成技能文件"（f2），但仓库本身没有一份可直接安装的 SKILL.md。加 `skills/` 目录：5-8 个按场景打包的电商技能（选品分析、Listing 生成、差评聚类、合规核查、定价决策），每个含 SKILL.md + 可直接粘贴的 Prompt + 数据纪律护栏。这正好兑现 README 的"Agent 时代可用"定位，也让"330+ Prompt"有了可计数的实体。

**RICH-2 三语术语表（一石二鸟）。** 见 R-4。既是翻译校验的锚点，也是读者的速查工具。放到 `src/resources/glossary.md`，三语对照 + 一句话定义。

**RICH-3 失败案例（契合"告诉你 AI 什么时候在编"的定位）。** 现有 5 个 case-studies 全是成功案例。补 2-3 个失败案例：AI 编造销量导致压错库存、自动调价翻车、翻译本地化事故——每个带"哪里断了 + 护栏本可以怎么拦住"。成功案例拉 star，失败案例建信任，后者是这个库的差异化人格。

**RICH-4 交互式导航。** "从哪开始"目前是静态表（角色 → 路径）。可做一个 HTML 决策器：选角色/规模/平台 → 输出推荐章节序列 + 对应 Notebook + 对应 skills。纯静态文件，放 `docs/` 构建产物外（如 `tools/`），不依赖后端。

**RICH-5 Prompt 模板库结构化 + 自动计数。** 现在 Prompt 散在各章正文里，"330+"无法复核。抽一个 `src/resources/prompt-library.md`（或 skills/ 的附属索引）：每个模板一个编号条目（场景/护栏类型/所在章节），M7 直接从它统计 Prompt 数并校验 README 声称——把"330+"变成可复算的事实。

**RICH-6 Notebook 附"验证过的输出"。** 18 个 notebook 已有，但读者无法预判运行结果。给每个 notebook 的 README 索引补一栏"示例输出/预期耗时/依赖版本"，至少对 top 5 常用的补上真实运行截图或输出样例。

---

## 3. 更新性（Update）

**U-1 verified 过期硬约束（把"校验日期"变成门禁）。** `model-matrix.md` 的模式（单点 + 日期）是全库最佳实践，但日期过期后**没有任何信号**——今天是 8/6，矩阵校验于 7/31，过不过期全凭自觉。新增检查：`claims: verified YYYY-MM` 距今超过 N 个月（建议 6）→ CI 失败并提示"复核或降级为 illustrative"。让"保质期"成为机器可执行的事实。

**U-2 外链刷新自动化。** M4 读缓存、合并前强制验活——很好，但缓存刷新是人工跑 `--probe-links`。加一个 weekly cron workflow：跑探测 → 对比 `link-status.json` → 有新死链就自动开 issue（标题"死链报告 YYYY-MM-DD"），人工合并刷新结果。平台链接（政策页、费率页）死得最快，这个必须自动化。

**U-3 CHANGELOG 门禁。** 8/4 的 17 个提交没进 CHANGELOG。加 CI 检查：内容变更（src/、i18n/、README*、roadmap/ 的 PR）必须带 CHANGELOG 的 [Unreleased] 条目，否则拒绝合并。成本一行正则，收益是更新历史不再靠自觉。

**U-4 易腐事实专项清单。** 全库最易腐的事实类别：平台佣金/费率、广告竞价基准、认证费用与时效、模型价格。建 `scripts/freshness-checklist.md`（或作为 M1 的输出）：列出每个类别落在哪些章节、复核节奏（月/季/半年）。与 U-1 联动——哪些 verified 标记该到期了，一键列出。

**U-5 社区过期报告渠道。** 现有 Issue 模板有 Prompt 提交、空链接报告，但读者发现"这个费率过时了"没有专门入口。加一个"数据过期报告"模板（过期内容位置 + 新值 + 来源），把读者变成哨兵——这是人力成本为零的更新机制。

---

## 4. 优先级与工作量

| 优先级 | 项 | 工作量 | 依赖 |
|--------|----|--------|------|
| **P0** | R-1 README 数字统一 + 纳入 M7 顶层树 | 0.5 天 | 无 |
| **P0** | R-2 修复或退役 tests/ | 0.5 天 | 无 |
| **P1** | U-1 verified 过期门禁 | 0.5 天 | 无 |
| **P1** | R-7 EVOLUTION_LOG 对齐 | 0.5 天 | 无 |
| **P1** | U-2 外链 cron + 自动 issue | 0.5 天 | 无 |
| **P1** | U-3 CHANGELOG 门禁 | 0.5 天 | 无 |
| **P1** | R-5 M2 内容非空壳检查 | 1 天 | 无 |
| **P1** | R-6 无证据断言检查 | 1 天 | 无 |
| **P2** | R-4 + RICH-2 术语表与翻译一致性 | 2-3 天 | 术语表先建 |
| **P2** | RICH-5 Prompt 模板库 + 自动计数 | 1-2 天 | R-1 的口径 |
| **P2** | RICH-1 skills/ 资产 | 3-5 天 | 内容创作 |
| **P2** | RICH-3 失败案例 ×2-3 | 2-3 天 | 内容创作 |
| **P2** | RICH-4 交互式导航 | 1-2 天 | 无 |
| **P3** | RICH-6 Notebook 输出样例 | 按需 | 需跑 notebook |
| **P3** | U-4/U-5 清单与报告模板 | 1 天 | U-1 落地后 |

**建议执行顺序**：P0 两件事先做（都是已证实的错误）→ P1 六个机制项（全部是脚本/CI 层面，不改内容）→ P2 内容工程 → P3。P0+P1 全部是"让门禁更严"，做完后 `verify_content.py` 从 10 项升到 14+ 项，仓库的严谨性从"承诺"变成"可执行"。

---

## 5. 一句话总结

严谨性的杠杆不在"多写内容"，而在**把每个已宣称的标准变成门禁**（README 数字、测试、翻译、过期日期都要可复算）；丰富性的杠杆在 **skills/ 与失败案例**（兑现 Agent 时代定位、强化差异化人格）；更新性的杠杆在**让过期有信号**（verified 门禁 + 外链 cron + 读者哨兵），而不是更勤奋地手动改。
